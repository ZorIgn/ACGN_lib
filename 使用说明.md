# ACGLib 私人书架

在 Windows 电脑运行，用电脑和同一局域网中的手机浏览器管理同一份作品记录。

## 启动

1. 将压缩包完整解压到固定目录。
2. 双击「启动.cmd」，等待浏览器打开，首次使用创建用户名和密码。
3. 手机连接同一 Wi-Fi，打开电脑的局域网地址，格式为 `http://电脑IP:8000/`。
4. 如果手机无法连接，双击「允许手机访问.cmd」，允许 Windows 管理员提示。规则仅允许本地网段访问书架端口。
5. 用完双击「停止.cmd」。电脑关机或休眠时，手机也无法访问。

电脑入口为 http://127.0.0.1:8000/ 。电脑当前 IP 可在 Windows 的 Wi-Fi 属性中查看。应用按需启动。

## 添加和评分

书架顶部的「类型」「状态」均可多选，勾选后直接更新结果。同一组内匹配任意所选项，再与另一组和名称搜索共同筛选。例如选择「小说、漫画」及「进行中、已完成」，会展示这两类中进行中或已完成的作品。更换条件从第一页展示；翻页和刷新保留筛选，「重置」清空当前文件夹内的筛选条件。

在「添加作品」中选择类型，输入名称，每行一部，最多 500 部，点击「搜索作品」。候选条目显示封面、名称和可用的年份、作者资料。同一组结果可以勾选多部作品，分别填写评分、状态、感想和观看链接后确认加入；取消勾选即可跳过。可输入 Bangumi 或 TMDB 条目链接；其他网站链接作为观看、阅读入口保存，再填写作品名称。

自定义作品填写名称即可创建。评分显示为 5 颗星，每半颗星对应 1 分，满分 10 分；可选择 0 分，或点击「清空」设为未评分。打分时，尚未设置的状态默认设为「已完成」；仍可手动改成进行中、暂停或清空。状态和评分都允许留空。点击书架中的作品可修改评分、状态、感想与观看链接。

每次检索都会建立一份导入草稿，每页展示 20 个输入名称。勾选、评分和记录会自动保存，翻页、刷新或离开后可在「添加作品 → 导入草稿」继续填写。「导入全部已选作品」会一次加入所有页面勾选的作品，已有书架记录保持原样。保存失败时页面会提示重试，当前浏览器保留待保存的修改。

「保存草稿并返回」、确认导入和作品详情中的保存，都会返回进入前的页面。书架的筛选与页码、推荐的类型与页码会保留，同一浏览器还会恢复搜索框内容和滚动位置。

支持混合类型清单，例如 `小说 | 仙逆 | 很喜欢`、`游戏 | 哈迪斯`，每行一项。纯名称使用上方选中的作品类型。「复用名单」会将原清单填回输入框，修改或补充后可再次检索。随本机数据提供的对话整理清单也是普通草稿，后续清单可直接粘贴建立。

## 发现作品

搜索框下的「为你推荐」根据当前类型的收藏、评分、作者与题材选择候选，同时参考公开热度。切换类型会重新加载；「热门作品」按公开热度和社区评分展示。偏好资料不足时先展示热门，识别到的已有收藏会被排除。

推荐卡片标注资料源、社区评分及推荐理由，最多展示 50 部，每页 12 部，通过「上一页」「下一页」翻页。切换类型或推荐模式从第一页展示；刷新和浏览器前进后退保留推荐页码。点击「加入清单」进入确认页，可填写自己的评分与状态后再入库。推荐异步加载，失败时可重试，期间仍能使用搜索。

个人偏好在本机计算，感想与个人评分不会上传到推荐资料源。作品目录、公开榜单和封面需要联网，资料缺项或来源差异会影响推荐效果。

## 收藏文件夹

在书架顶部点击「管理文件夹」，输入名称并新建。打开书架中的作品，在「收藏文件夹」中勾选分类后保存；同一作品可以属于多个文件夹，取消勾选即可移出对应分类。

点击文件夹名称查看其中的作品，类型、状态与名称筛选仍然有效。「全部作品」查看整个书架。进入文件夹后，可在「管理文件夹」中改名或删除；删除文件夹只删除分类关系，作品及评分仍保留。

## Steam

在「Steam 同步」填写 SteamID64 和自己的 Steam Web API 密钥。资料与游戏详情需要公开，游玩时长需要允许读取。点击「保存并立即同步」，稍后刷新查看结果。勾选每天同步后，应用运行期间会自动执行。

Steam 同步更新累计时长与可用封面，默认填写商店链接。有游玩时长的游戏默认「进行中」，尚未游玩的默认「想看 / 想玩」；未手动修改的待玩游戏，在首次游玩后自动转为进行中。你填写的评分、状态、感想和自定义链接会保留，链接也可以清空。API 不提供通关结论。

## 数据与备份

个人数据和同步设置保存在 `src/db/`，应用密钥在 `.env`，Steam 密钥以加密形式保存在数据库。备份前停止应用，复制整个应用目录；恢复时解压到固定目录后启动。保留 `.env` 才能读取已保存的 Steam 密钥。

启动日志位于 `src/db/runtime-error.log`。同时运行多个副本会占用相同的 8000 端口。

## 源码与许可

基于 [Yamtrack v0.26.3](https://github.com/FuzzyGrim/Yamtrack/tree/v0.26.3)，采用 AGPL-3.0，完整许可见 `LICENSE`，应用源码与构建文件见 `source.zip`。运行目录包含 Python 及其许可证，依赖的许可证保留在 `runtime/site-packages` 对应分发目录中。

小说、漫画、动画、剧集和游戏名称资料来自 [Bangumi API](https://github.com/bangumi/api)，小说检索以 [QQ 阅读](https://book.qq.com/) 公开目录补充，电影资料来自 [TMDB](https://www.themoviedb.org/)。Steam 使用 [GetOwnedGames](https://partner.steamgames.com/doc/webapi/IPlayerService#GetOwnedGames)。搜索和封面加载需要网络，私人记录保存在本机。
